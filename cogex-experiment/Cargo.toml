[package]
name = "cogex-experiment"
version = "0.1.0"
edition = "2024"

[dependencies]
cogex-core = { path = "../cogex-core" }
cogex-timing = { path = "../cogex-timing" }
rand = "0.9"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

